[project]
name = "unipath-ai"
version = "0.1.0"
description = "UniPath AI FastAPI backend"
requires-python = ">=3.11"
dependencies = [
    "fastapi[standard]",
]

[tool.fastapi]
entrypoint = "app.main:app"